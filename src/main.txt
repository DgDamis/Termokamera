// Change this file's filename extension (suffix) to have a flashable clearing code

#include <Arduino.h>
#include "SPI.h"
#include "Wire.h"

void setup() {
  // put your setup code here, to run once:
}

void loop() {
  // put your main code here, to run repeatedly:
}